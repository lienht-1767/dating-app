<li>
  <div class="row">
    <div class="<%= user.id == message.user_id ? "message-sent" : "message-received" %>">
      <%= message.body %> <br>
      <span><%= message.created_at %></span>
    </div>
  </div>
</li>
